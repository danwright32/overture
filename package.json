{
  "name": "overture-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Overture scout engine: discovers and ranks performances, writes the results handoff the native macOS app ingests.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "export": "tsx scripts/export-results.ts",
    "seed": "tsx scripts/seed-prospects.ts",
    "import-history": "tsx scripts/import-history.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "csv-parse": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.22.4",
    "typescript": "^5",
    "vitest": "^4.1.9"
  }
}
